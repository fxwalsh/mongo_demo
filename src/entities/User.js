export default class User {
    constructor(id, name,  email,password, dob, type ) {
      this.id = id;
      this.name = name;
      this.email = email;
      this.password = password;
      this.dob = dob;
      this. type = type;
    }
  }